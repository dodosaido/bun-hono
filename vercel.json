{
    "version": 2,
    "builds": [
        {
            "src": "src/index.ts",
            "use": "@vercel/bun",
            "config": {
                "includeFiles": "views/**"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "src/index.ts"
        }
    ]
}
